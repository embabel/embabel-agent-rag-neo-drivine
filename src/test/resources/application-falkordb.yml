# FalkorDB test profile
# Activate with @ActiveProfiles("falkordb")
# Uses testcontainer by default; set USE_LOCAL_FALKORDB=true for local instance

database:
  datasources:
    graph:
      type: FALKORDB
      host: localhost
      port: 6379
      user-name: ""
      password: ""
      database-name: neo4j